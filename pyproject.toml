[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
markers = [
    "integration: tests that connect to a real database",
]

[tool.ruff]
line-length = 100

[tool.mypy]
ignore_missing_imports = true
strict = true
